/* Defined if we have MPI */
#cmakedefine HAVE_MPI

/* Defined if we have HDF5 */
#cmakedefine HAVE_HDF5

/* Defined if the OPar is compiled in 1D configuration */
#cmakedefine ONE_DIMENSIONAL

/* Defined if the OPar is compiled in 2D configuration */
#cmakedefine TWO_DIMENSIONAL

/* Defined if the OPar is compiled in 3D configuration */
#cmakedefine THREE_DIMENSIONAL

/* Name of the project */
#define PROJECT_NAME @PROJECT_NAME@

/* Version of the project*/
#define PROJECT_VERSION @PROJECT_VERSION@
#define PROJECT_VERSION_MAJOR @PROJECT_VERSION_MAJOR@
#define PROJECT_VERSION_MINOR @PROJECT_VERSION_MINOR@
#define PROJECT_VERSION_PATCH @PROJECT_VERSION_PATCH@
